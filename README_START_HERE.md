# LiteRaceSegNet Personal Research Edition — Start Here

This is the recommended reading order for the v15.2 public portal merged package.

## 1. First screen

Open:

```text
index.html
```

This is the GitHub Pages-ready public entry. It presents the current identity as **LiteRaceSegNet Personal Research Edition** and the edition as **v15.2 Personal Research Edition**.

## 2. Research-extension charter

Read:

```text
docs/research_extension_v15_2.md
```

This explains how LiteRaceSegNet core remains the road-damage segmentation thesis/capstone core while HPLS-Eval and Precision Mini-Lab become separate follow-up research axes.

## 3. Output-stability metrics

Read:

```text
docs/output_stability_metrics.md
pages/output-stability.html
```

These files define mask area drift, boundary IoU drift, connected component count, centroid shift, target-only region area, reference-only region area, runtime backend, and fallback status.

## 4. Precision Mini-Lab protocol

Read:

```text
docs/precision_mini_lab_protocol.md
hpls-eval/precision-mini-lab/experiment_matrix.md
hpls-eval/precision-mini-lab/result_template.md
```

These files define the FP32 / FP16 / INT8 segmentation-output stability pilot protocol and result recording template.

## 5. Claim boundary

Read:

```text
docs/claim_boundary_for_extension.md
pages/license.html
```

The extension is a follow-up research program. It does not replace the original LiteRaceSegNet core claim and does not create clinical, diagnostic, deployment, or complete benchmark evidence.

## 6. Current public identity

This public surface uses the LiteRaceSegNet Personal Research Edition identity. Follow-up pages are included as review aids, not as separate product claims.

## GitHub Pages main visual

The `index.html` first screen displays:

```text
assets/literacesegnet_public_portal_main.png
```

This asset uses only the exact user-uploaded public portal image. It is navigation/branding material for the GitHub Pages main screen, not experimental evidence or a measured result.
